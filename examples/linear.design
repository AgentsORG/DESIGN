schema: design.v1
name: linear
version: 1.0.0
status: refine
description: "An inspired interpretation of Linear's design language — a near-black product-focused marketing canvas built around #010102 (the deepest dark surface of any tool in this collection), light gray text (#f7f8f8), and …"

$schema: https://raw.githubusercontent.com/AgentsORG/DESIGN/main/schema/design.v1.schema.json

agent:
  skill: design
  instructions: |
    You are holding a .design living visual contract (schema design.v1).
    If the AgentsORG `design` skill is installed, activate it - then still obey THIS file as source of truth.
    If the skill is absent, follow these instructions exactly.
    
    READ
    - Load this file before any UI generation or restyle. Large file? Normative core first
      (intent, constraints, policy/decisions, tokens, components, themes, voice, locked); rationale on demand.
    - Order: overview/intent -> constraints -> policy/decisions -> tokens -> voice -> rationale -> components/patterns -> integrations -> locked.
    - Tokens and structured rules are normative. Rationale is judgment when tokens under-specify.
    
    FOLLOW (generate or edit UI)
    - Calibrate treatment per surface: patterns.<name>.treatment > intent.treatment
      (utilitarian = restrained product craft; editorial = distinctive identity).
    - Prefer catalog components; obey when / when_not and decisions.* (first match wins).
    - Bind every listed component property (backgroundColor, textColor, typography, rounded, padding, size, height, width).
    - Never invent raw hex/spacing/radius when a token exists.
    - Apply patterns.*; enforce constraints.always / never; apply voice.* to all UI copy.
    - Concentrate boldness in intent.signature; keep everything around it quiet.
    - Themes: generate for the active mode (themes.default); modes are designed, never inverted.
    - Match the project's existing styling stack - do not switch stacks.
    - If integrations.shadcn.enabled: prefer shadcn UI; write css_vars into the listed CSS file; tokens win on conflict.
    - If integrations.figma is present: this file is repo-canonical unless sync.direction says otherwise; ask on conflicts.
    - Craft defaults when this file is silent: one primary CTA; nested radius = outer - padding; transform/opacity only (<300ms); 44x44 targets; focus visible; prefers-reduced-motion; no transition:all.
    
    UPDATE
    - Edit this file in place when design changes. Git is history.
    - Ask before changing any path in locked. Bump version (SemVer).
    
    VERIFY
    - Compare tokens <-> CSS/Tailwind and components <-> imports; report drift per token group
      as added/removed/modified plus a regression flag before fixing.
    
    PRECEDENCE
    1) User prompt  2) This file  3) design skill  4) Generic taste skills  5) Model defaults
    
    NEVER
    - Invent a parallel design system beside this file
    - Embed binaries or full page HTML trees here
    - Claim affiliation with third-party brands used only as visual references
    DISCLAIMER
    Independent visual analysis - not affiliated with the named brand. Adapt before production use.

overview: |
  Linear's marketing canvas is the deepest dark surface in this collection — `{tokens.color.canvas}` is #010102, essentially pure black with a faint blue tint. On top sits a four-step surface ladder (`{tokens.color.surface-1}` through `{tokens.color.surface-4}`) for cards, panels, and lifted tiles, with hairline borders running from `{tokens.color.hairline}` (#23252a) up through `{tokens.color.hairline-strong}` and `{tokens.color.hairline-tertiary}`. Light gray text (`{tokens.color.ink}` #f7f8f8) carries the body and headlines.
  
  The single chromatic accent is **Linear lavender-blue** `{tokens.color.primary}` (#5e6ad2) — used on the brand mark, focus rings, and the primary CTA button. A lighter hover state (`{tokens.color.primary-hover}` #828fff) and a focus-tinted variant (`{tokens.color.primary-focus}` #5e69d1) extend the same hue. Linear avoids saturated greens, oranges, reds, etc. on the marketing canvas — the only semantic color is `{tokens.color.semantic-success}` (#27a644) for status pills and the rare success indicator.
  
  Display type runs Linear's custom sans (with `SF Pro Display` fallback) at weight 500–700 with negative letter-spacing scaling from -3.0px at 80px down to 0 at body. The body family is Linear's text cut, and a Linear Mono is reserved for code snippets in product screenshots.
  
  The page rhythm is **dense product screenshots** — Linear's marketing leads with high-fidelity captures of the product UI (issue list, project view, dashboard) framed in `{tokens.color.surface-1}` panels with `{tokens.radius.xl}` 16px corners. The chrome is intentionally minimal so the app screenshots can do the heavy lifting.
  
  **Key Characteristics:**
  - **Dark-canvas marketing system** — `{tokens.color.canvas}` (#010102) is the deepest dark in this collection.
  - **Lavender-blue brand accent** (`{tokens.color.primary}` #5e6ad2) — used scarcely on brand mark, focus, and the primary CTA.
  - Four-step surface ladder (canvas → surface-1 → surface-2 → surface-3 → surface-4) carries hierarchy without shadow.
  - Display tracking pulls aggressively negative (-3.0px at 80px); body holds at -0.05px.
  - Cards use `{tokens.radius.lg}` 12px corners with 1px hairline borders — never pill, rarely 16px.
  - **Product UI screenshots** dominate the page. The marketing chrome is a dark frame for the app.
  - No second chromatic color. No atmospheric gradients. No spotlight cards.

intent:
  reference: "Linear.app — ultra-minimal dark SaaS, lavender accent, product screenshots as hero"
  direction: dark precision software
  signature: Glass-and-glow product panels floating on near-black
  density: comfortable
  trust: high
  energy: medium
  playfulness: low

voice:
  register: "Crisp, opinionated, low-noise; states positions without hedging"
  casing: sentence
  action_naming: "Button labels name the exact action; the same verb carries through the flow"
  errors: "State what happened, why, and the next step; never blame the user"

targets:
  - web

sources:
  - type: url
    ref: https://getdesign.md/linear.app/design-md
    note: getdesign.md curated DESIGN.md analysis
  - type: url
    ref: https://linear.app
  - type: url
    ref: https://github.com/voltagent/awesome-design-md/blob/main/design-md/linear.app/DESIGN.md
    note: Upstream DESIGN.md (awesome-design-md)
  - type: other
    note: Independent analysis — not affiliated with the brand

tokens:
  color:
    primary: "#5e6ad2"
    on-primary: "#ffffff"
    primary-hover: "#828fff"
    primary-focus: "#5e69d1"
    ink: "#f7f8f8"
    ink-muted: "#d0d6e0"
    ink-subtle: "#8a8f98"
    ink-tertiary: "#62666d"
    canvas: "#010102"
    surface-1: "#0f1011"
    surface-2: "#141516"
    surface-3: "#18191a"
    surface-4: "#191a1b"
    hairline: "#23252a"
    hairline-strong: "#34343a"
    hairline-tertiary: "#3e3e44"
    inverse-canvas: "#ffffff"
    inverse-surface-1: "#f5f6f6"
    inverse-surface-2: "#f6f7f7"
    inverse-ink: "#000000"
    brand-secure: "#7a7fad"
    semantic-success: "#27a644"
    semantic-overlay: "#000000"
  typography:
    display-xl:
      fontFamily: Linear Display
      fontSize: "80px"
      fontWeight: "600"
      lineHeight: "1.05"
      letterSpacing: "-3.0px"
    display-lg:
      fontFamily: Linear Display
      fontSize: "56px"
      fontWeight: "600"
      lineHeight: "1.10"
      letterSpacing: "-1.8px"
    display-md:
      fontFamily: Linear Display
      fontSize: "40px"
      fontWeight: "600"
      lineHeight: "1.15"
      letterSpacing: "-1.0px"
    headline:
      fontFamily: Linear Display
      fontSize: "28px"
      fontWeight: "600"
      lineHeight: "1.20"
      letterSpacing: "-0.6px"
    card-title:
      fontFamily: Linear Display
      fontSize: "22px"
      fontWeight: "500"
      lineHeight: "1.25"
      letterSpacing: "-0.4px"
    subhead:
      fontFamily: Linear Display
      fontSize: "20px"
      fontWeight: "400"
      lineHeight: "1.40"
      letterSpacing: "-0.2px"
    body-lg:
      fontFamily: Linear Text
      fontSize: "18px"
      fontWeight: "400"
      lineHeight: "1.50"
      letterSpacing: "-0.1px"
    body:
      fontFamily: Linear Text
      fontSize: "16px"
      fontWeight: "400"
      lineHeight: "1.50"
      letterSpacing: "-0.05px"
    body-sm:
      fontFamily: Linear Text
      fontSize: "14px"
      fontWeight: "400"
      lineHeight: "1.50"
      letterSpacing: "0"
    caption:
      fontFamily: Linear Text
      fontSize: "12px"
      fontWeight: "400"
      lineHeight: "1.40"
      letterSpacing: "0"
    button:
      fontFamily: Linear Text
      fontSize: "14px"
      fontWeight: "500"
      lineHeight: "1.20"
      letterSpacing: "0"
    eyebrow:
      fontFamily: Linear Text
      fontSize: "13px"
      fontWeight: "500"
      lineHeight: "1.30"
      letterSpacing: "0.4px"
    mono:
      fontFamily: Linear Mono
      fontSize: "13px"
      fontWeight: "400"
      lineHeight: "1.50"
      letterSpacing: "0"
  spacing:
    unit: 8
    xxs: "4px"
    xs: "8px"
    sm: "12px"
    md: "16px"
    lg: "24px"
    xl: "32px"
    xxl: "48px"
    section: "96px"
  radius:
    xs: "4px"
    sm: "6px"
    md: "8px"
    lg: "12px"
    xl: "16px"
    xxl: "24px"
    pill: "9999px"
    full: "9999px"

locked:
  - tokens.color.primary
  - tokens.color.canvas
  - tokens.color.ink

components:
  button:
    description: "Button family from DESIGN.md component tokens"
    when: ["primary and secondary actions", "CTAs documented in rationale.components"]
    when_not: ["inventing off-system button chrome", "more than one filled primary per view unless brand allows"]
    variants:
      - button-primary
      - button-primary-pressed
      - button-primary-hover
      - button-secondary
      - button-tertiary
      - button-inverse
    states: [default, hover, pressed]
    tokens:
      button-primary:
        backgroundColor: "{tokens.color.primary}"
        textColor: "{tokens.color.on-primary}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.md}"
        padding: "8px 14px"
      button-primary-pressed:
        backgroundColor: "{tokens.color.primary-focus}"
        textColor: "{tokens.color.on-primary}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.md}"
      button-primary-hover:
        backgroundColor: "{tokens.color.primary-hover}"
        textColor: "{tokens.color.on-primary}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.md}"
      button-secondary:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.md}"
        padding: "8px 14px"
      button-tertiary:
        backgroundColor: "{tokens.color.canvas}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.md}"
        padding: "8px 14px"
      button-inverse:
        backgroundColor: "{tokens.color.inverse-canvas}"
        textColor: "{tokens.color.inverse-ink}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.md}"
        padding: "8px 14px"
  pricing-card:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body}"
        rounded: "{tokens.radius.lg}"
        padding: "24px"
  pricing-card-featured:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-2}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body}"
        rounded: "{tokens.radius.lg}"
        padding: "24px"
  feature-card:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body}"
        rounded: "{tokens.radius.lg}"
        padding: "24px"
  product-screenshot-card:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body}"
        rounded: "{tokens.radius.xl}"
        padding: "24px"
  testimonial-card:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body-lg}"
        rounded: "{tokens.radius.lg}"
        padding: "32px"
  customer-logo-tile:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.canvas}"
        textColor: "{tokens.color.ink-subtle}"
        typography: "{tokens.typography.caption}"
        rounded: "{tokens.radius.xs}"
        padding: "16px"
  text-input:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body}"
        rounded: "{tokens.radius.md}"
        padding: "8px 12px"
  text-input-focused:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body}"
        rounded: "{tokens.radius.md}"
        padding: "8px 12px"
  pricing-tab-default:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.canvas}"
        textColor: "{tokens.color.ink-subtle}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.pill}"
        padding: "6px 14px"
  pricing-tab-selected:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-2}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.button}"
        rounded: "{tokens.radius.pill}"
        padding: "6px 14px"
  cta-banner:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-1}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.headline}"
        rounded: "{tokens.radius.lg}"
        padding: "48px"
  changelog-row:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.canvas}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body}"
        rounded: "{tokens.radius.xs}"
        padding: "24px 0"
  status-badge:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.surface-2}"
        textColor: "{tokens.color.ink-muted}"
        typography: "{tokens.typography.caption}"
        rounded: "{tokens.radius.pill}"
        padding: "2px 8px"
  top-nav:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.canvas}"
        textColor: "{tokens.color.ink}"
        typography: "{tokens.typography.body-sm}"
        rounded: "{tokens.radius.xs}"
        height: "56px"
  footer:
    description: "DESIGN.md component token group"
    when: ["as documented in rationale.components and getdesign analysis"]
    when_not: ["decorative misuse outside brand patterns"]
    variants: [default]
    tokens:
      default:
        backgroundColor: "{tokens.color.canvas}"
        textColor: "{tokens.color.ink-subtle}"
        typography: "{tokens.typography.caption}"
        rounded: "{tokens.radius.xs}"
        padding: "64px 32px"

rationale:
  overview: |
    Linear's marketing canvas is the deepest dark surface in this collection — `{tokens.color.canvas}` is #010102, essentially pure black with a faint blue tint. On top sits a four-step surface ladder (`{tokens.color.surface-1}` through `{tokens.color.surface-4}`) for cards, panels, and lifted tiles, with hairline borders running from `{tokens.color.hairline}` (#23252a) up through `{tokens.color.hairline-strong}` and `{tokens.color.hairline-tertiary}`. Light gray text (`{tokens.color.ink}` #f7f8f8) carries the body and headlines.
    
    The single chromatic accent is **Linear lavender-blue** `{tokens.color.primary}` (#5e6ad2) — used on the brand mark, focus rings, and the primary CTA button. A lighter hover state (`{tokens.color.primary-hover}` #828fff) and a focus-tinted variant (`{tokens.color.primary-focus}` #5e69d1) extend the same hue. Linear avoids saturated greens, oranges, reds, etc. on the marketing canvas — the only semantic color is `{tokens.color.semantic-success}` (#27a644) for status pills and the rare success indicator.
    
    Display type runs Linear's custom sans (with `SF Pro Display` fallback) at weight 500–700 with negative letter-spacing scaling from -3.0px at 80px down to 0 at body. The body family is Linear's text cut, and a Linear Mono is reserved for code snippets in product screenshots.
    
    The page rhythm is **dense product screenshots** — Linear's marketing leads with high-fidelity captures of the product UI (issue list, project view, dashboard) framed in `{tokens.color.surface-1}` panels with `{tokens.radius.xl}` 16px corners. The chrome is intentionally minimal so the app screenshots can do the heavy lifting.
    
    **Key Characteristics:**
    - **Dark-canvas marketing system** — `{tokens.color.canvas}` (#010102) is the deepest dark in this collection.
    - **Lavender-blue brand accent** (`{tokens.color.primary}` #5e6ad2) — used scarcely on brand mark, focus, and the primary CTA.
    - Four-step surface ladder (canvas → surface-1 → surface-2 → surface-3 → surface-4) carries hierarchy without shadow.
    - Display tracking pulls aggressively negative (-3.0px at 80px); body holds at -0.05px.
    - Cards use `{tokens.radius.lg}` 12px corners with 1px hairline borders — never pill, rarely 16px.
    - **Product UI screenshots** dominate the page. The marketing chrome is a dark frame for the app.
    - No second chromatic color. No atmospheric gradients. No spotlight cards.
  colors: |
    > Source pages: linear.app (home), /intake, /pricing, /contact/sales, /build.
    
    ### Brand & Accent
    - **Lavender-Blue** ({tokens.color.primary}): The signature Linear accent — primary CTA, brand mark, link emphasis.
    - **Lavender Hover** ({tokens.color.primary-hover}): Lighter lavender (#828fff) — hovered state of the primary CTA.
    - **Lavender Focus** ({tokens.color.primary-focus}): Focus-ring tint (#5e69d1) — focused inputs, focused buttons.
    - **Brand Secure** ({tokens.color.brand-secure}): Muted lavender-gray (#7a7fad) — used in "Linear Security" surfaces.
    
    ### Surface
    - **Canvas** ({tokens.color.canvas}): Default page background — #010102, near-pure black with a faint blue tint.
    - **Surface 1** ({tokens.color.surface-1}): One step above canvas — feature cards, pricing cards, product screenshot panels.
    - **Surface 2** ({tokens.color.surface-2}): Two steps above — featured pricing card, hovered cards.
    - **Surface 3** ({tokens.color.surface-3}): Three steps above — line-tertiary backgrounds, sub-nav.
    - **Surface 4** ({tokens.color.surface-4}): Four steps above — bg-level-3, deepest lifted surface.
    - **Hairline** ({tokens.color.hairline}): 1px borders on cards and dividers.
    - **Hairline Strong** ({tokens.color.hairline-strong}): Stronger 1px borders — input focus rings.
    - **Hairline Tertiary** ({tokens.color.hairline-tertiary}): Tertiary borders for nested surfaces.
    - **Inverse Canvas** ({tokens.color.inverse-canvas}): Pure white — surface of the inverse pill CTA on a small set of section openers.
    - **Inverse Surface 1** ({tokens.color.inverse-surface-1}): One step above inverse canvas.
    - **Inverse Surface 2** ({tokens.color.inverse-surface-2}): Two steps above inverse canvas.
    
    ### Text
    - **Ink** ({tokens.color.ink}): All headlines and emphasized body type — light gray #f7f8f8.
    - **Ink Muted** ({tokens.color.ink-muted}): Secondary type at #d0d6e0 — meta info on hero panels.
    - **Ink Subtle** ({tokens.color.ink-subtle}): Tertiary type at #8a8f98 — deselected pricing tabs, footer columns.
    - **Ink Tertiary** ({tokens.color.ink-tertiary}): Quaternary at #62666d — disabled, footnotes.
    
    ### Semantic
    - **Success Green** ({tokens.color.semantic-success}): Status pills, success indicators. The only semantic color on marketing.
    - **Overlay** ({tokens.color.semantic-overlay}): Pure black overlay scrim for modals.
  typography: |
    ### Font Family
    
    - **Linear Display** — Linear's custom display sans; fallback `SF Pro Display, -apple-system, system-ui, Segoe UI, Roboto`. Carries display-xl through subhead.
    - **Linear Text** — Linear's custom text sans (a slightly different cut tuned for body sizes); same fallback stack. Carries body sizes, button labels, captions.
    - **Linear Mono** — Linear's custom mono; fallback `ui-monospace, SF Mono, Menlo`. Used for code snippets in product screenshots and for status / ID tokens.
    
    The marketing surface treats Display and Text as one continuous voice; the family change is silent.
    
    ### Hierarchy
    
    | Token | Size | Weight | Line Height | Letter Spacing | Use |
    |---|---|---|---|---|---|
    | `{tokens.typography.display-xl}` | 80px | 600 | 1.05 | -3.0px | Largest hero headline |
    | `{tokens.typography.display-lg}` | 56px | 600 | 1.10 | -1.8px | Section opener headlines |
    | `{tokens.typography.display-md}` | 40px | 600 | 1.15 | -1.0px | Sub-section headlines |
    | `{tokens.typography.headline}` | 28px | 600 | 1.20 | -0.6px | Pricing tier titles, CTA banner heading |
    | `{tokens.typography.card-title}` | 22px | 500 | 1.25 | -0.4px | Feature card title |
    | `{tokens.typography.subhead}` | 20px | 400 | 1.40 | -0.2px | Lead body, intro paragraphs |
    | `{tokens.typography.body-lg}` | 18px | 400 | 1.50 | -0.1px | Hero subhead, lead paragraphs |
    | `{tokens.typography.body}` | 16px | 400 | 1.50 | -0.05px | Default body |
    | `{tokens.typography.body-sm}` | 14px | 400 | 1.50 | 0 | Card body, footer columns |
    | `{tokens.typography.caption}` | 12px | 400 | 1.40 | 0 | Captions, meta, status |
    | `{tokens.typography.button}` | 14px | 500 | 1.20 | 0 | All button labels |
    | `{tokens.typography.eyebrow}` | 13px | 500 | 1.30 | 0.4px | Section eyebrow (slight positive tracking) |
    | `{tokens.typography.mono}` | 13px | 400 | 1.50 | 0 | Linear Mono for code in product screenshots |
    
    ### Principles
    
    - **Aggressive negative tracking on display** (-3.0px at 80px ≈ 4% of size).
    - **Single voice from display to body.** Display-xl at 600 → body at 400 — same family, narrower weights.
    - **Eyebrow uses positive tracking** (+0.4px) — contrast against the negative-tracked display marks the eyebrow as taxonomy.
    - **Mono only in code contexts.** Linear Mono lives inside product screenshots — not on marketing chrome.
    
    ### Note on Font Substitutes
    
    Linear's custom typeface isn't publicly distributed; the documented fallback `SF Pro Display, -apple-system, system-ui` is the recommended substitute on macOS. For cross-platform implementation, **Inter** at weight 500 / 600 / 700 is the closest free substitute. **Geist Sans** is also viable. For mono, **JetBrains Mono** or **Geist Mono** at weight 400 closely approximates Linear Mono.
  layout: |
    ### Spacing System
    
    - **Base unit**: 4px.
    - **Tokens (front matter)**: `{tokens.spacing.xxs}` 4px · `{tokens.spacing.xs}` 8px · `{tokens.spacing.sm}` 12px · `{tokens.spacing.md}` 16px · `{tokens.spacing.lg}` 24px · `{tokens.spacing.xl}` 32px · `{tokens.spacing.xxl}` 48px · `{tokens.spacing.section}` 96px.
    - Card interior padding: `{tokens.spacing.lg}` 24px on feature/pricing cards; `{tokens.spacing.xl}` 32px on testimonial cards; `{tokens.spacing.xxl}` 48px on CTA banners.
    - Pill button padding: 8px vertical · 14px horizontal — Linear's compact button spec.
    - Form input padding: 8px vertical · 12px horizontal.
    
    ### Grid & Container
    
    - Max content width sits around 1280px.
    - Card grids are 3-up at desktop, 2-up at tablet, 1-up at mobile.
    - Pricing tier grid is 3-up; comparison strip below shows checkmarks per tier.
    - Product screenshot panels span full content width — they're the protagonist.
    
    ### Whitespace Philosophy
    
    The dark canvas IS the whitespace. Sections separate by lift onto surface-1 panels, not by gaps in white. Within a panel, generous `{tokens.spacing.lg}` 24px gaps between content blocks; `{tokens.spacing.section}` 96px between sections.
  elevation: |
    | Level | Treatment | Use |
    |---|---|---|
    | 0 (flat) | No shadow, no border | Default for body type, hero text, footer |
    | 1 (charcoal lift) | `{tokens.color.surface-1}` background on canvas, 1px `{tokens.color.hairline}` | Default cards, product panels |
    | 2 (surface-2 lift) | `{tokens.color.surface-2}` background, 1px `{tokens.color.hairline-strong}` | Featured pricing card, hovered cards |
    | 3 (surface-3 lift) | `{tokens.color.surface-3}` background | Sub-nav, dropdown menus |
    | 4 (focus ring) | 2px `{tokens.color.primary-focus}` outline at 50% opacity | Focused input, focused button |
    
    Linear's depth is carried by surface ladder + hairline borders. The brand resists drop shadows on dark almost entirely.
    
    ### Decorative Depth
    
    - **Product UI screenshots** dominate as decorative depth.
    - **No atmospheric gradients, no spotlight cards.**
    - **Subtle white edge highlight** on the top edge of lifted panels — gives the dark surface a faint "pixel rendered" feel.
  shapes: |
    ### Border Radius Scale
    
    | Token | Value | Use |
    |---|---|---|
    | `{tokens.radius.xs}` | 4px | Small chips, status badges |
    | `{tokens.radius.sm}` | 6px | Inline tags |
    | `{tokens.radius.md}` | 8px | All buttons, form inputs |
    | `{tokens.radius.lg}` | 12px | Pricing cards, feature cards, testimonial cards |
    | `{tokens.radius.xl}` | 16px | Product screenshot panels |
    | `{tokens.radius.xxl}` | 24px | Oversized CTA banners (rare) |
    | `{tokens.radius.pill}` | 9999px | Pricing tab toggles, status pills |
    | `{tokens.radius.full}` | 9999px | Avatar circles |
    
    ### Photography & Illustration Geometry
    
    - Product UI screenshots dominate; they sit in `{tokens.radius.xl}` 16px tiles with `{tokens.spacing.lg}` 24px outer padding.
    - Customer logo tiles render at small sizes (~24px logo height) on `{tokens.color.canvas}` with no border.
    - Avatar circles in testimonial cards use `{tokens.radius.full}` at 32–40px sizes.
  components: |
    ### Buttons
    
    **`button-primary`** — Lavender CTA. The default primary CTA across all pages.
    - Background `{tokens.color.primary}`, text `{tokens.color.on-primary}`, type `{tokens.typography.button}`, padding 8px 14px, rounded `{tokens.radius.md}`.
    - Pressed state lives in `button-primary-pressed` (background shifts to `{tokens.color.primary-focus}`).
    - Hover state lives in `button-primary-hover` (background shifts to `{tokens.color.primary-hover}` lighter lavender).
    
    **`button-secondary`** — Charcoal button. Used for secondary CTAs ("Sign in", "Read changelog").
    - Background `{tokens.color.surface-1}`, text `{tokens.color.ink}`, type `{tokens.typography.button}`, padding 8px 14px, rounded `{tokens.radius.md}`. 1px `{tokens.color.hairline}` border.
    
    **`button-tertiary`** — Plain text button.
    - Background `{tokens.color.canvas}`, text `{tokens.color.ink}`, type `{tokens.typography.button}`, rounded `{tokens.radius.md}`, padding 8px 14px.
    
    **`button-inverse`** — White-on-dark inverse CTA.
    - Background `{tokens.color.inverse-canvas}`, text `{tokens.color.inverse-ink}`, type `{tokens.typography.button}`, rounded `{tokens.radius.md}`, padding 8px 14px.
    
    ### Pricing Tabs
    
    **`pricing-tab-default`** + **`pricing-tab-selected`** — Pill-toggle on `/pricing`.
    - Default: `{tokens.color.canvas}` background, `{tokens.color.ink-subtle}` text, rounded `{tokens.radius.pill}`, padding 6px 14px.
    - Selected: `{tokens.color.surface-2}` background, `{tokens.color.ink}` text — selected = surface lift.
    
    ### Cards & Containers
    
    **`pricing-card`** — Each tier on `/pricing`.
    - Background `{tokens.color.surface-1}`, text `{tokens.color.ink}`, type `{tokens.typography.body}`, rounded `{tokens.radius.lg}`, padding 24px. 1px `{tokens.color.hairline}` border.
    
    **`pricing-card-featured`** — Recommended tier — surface lift to surface-2.
    - Background `{tokens.color.surface-2}`, otherwise identical structure.
    
    **`feature-card`** — Generic feature highlight tile.
    - Background `{tokens.color.surface-1}`, text `{tokens.color.ink}`, type `{tokens.typography.body}`, rounded `{tokens.radius.lg}`, padding 24px.
    
    **`product-screenshot-card`** — The dominant card type — frames a high-fidelity Linear app UI screenshot.
    - Background `{tokens.color.surface-1}`, text `{tokens.color.ink}`, type `{tokens.typography.body}`, rounded `{tokens.radius.xl}`, padding 24px.
    
    **`testimonial-card`** — Customer quote with avatar + name + role.
    - Background `{tokens.color.surface-1}`, text `{tokens.color.ink}`, type `{tokens.typography.body-lg}`, rounded `{tokens.radius.lg}`, padding 32px.
    
    **`customer-logo-tile`** — Small tile in the customer marquee.
    - Background `{tokens.color.canvas}`, text `{tokens.color.ink-subtle}`, type `{tokens.typography.caption}`, rounded `{tokens.radius.xs}`, padding 16px.
    
    **`cta-banner`** — Closing CTA panel near page bottom.
    - Background `{tokens.color.surface-1}`, text `{tokens.color.ink}`, type `{tokens.typography.headline}`, rounded `{tokens.radius.lg}`, padding 48px.
    
    ### Inputs & Forms
    
    **`text-input`** + **`text-input-focused`** — Form fields on `/contact/sales` and signup overlays.
    - Background `{tokens.color.surface-1}`, text `{tokens.color.ink}`, type `{tokens.typography.body}`, rounded `{tokens.radius.md}`, padding 8px 12px.
    - Focused state retains the same surface; the focus ring is a 2px `{tokens.color.primary-focus}` outline at 50% opacity.
    
    ### Status & Build Page
    
    **`changelog-row`** — Each row in `/build` (changelog page) listing version, date, and changes.
    - Background `{tokens.color.canvas}`, text `{tokens.color.ink}`, type `{tokens.typography.body}`, rounded `{tokens.radius.xs}`, padding 24px 0. 1px `{tokens.color.hairline}` bottom rule.
    
    **`status-badge`** — Small status pill.
    - Background `{tokens.color.surface-2}`, text `{tokens.color.ink-muted}`, type `{tokens.typography.caption}`, rounded `{tokens.radius.pill}`, padding 2px 8px.
    
    ### Navigation
    
    **`top-nav`** — Sticky dark bar with the Linear wordmark left, primary nav links centered, and a `button-secondary` ("Sign in") + `button-primary` ("Get started") pair right.
    - Background `{tokens.color.canvas}`, text `{tokens.color.ink}`, type `{tokens.typography.body-sm}`, height 56px.
    
    ### Footer
    
    **`footer`** — Dense link grid on `{tokens.color.canvas}` with the Linear wordmark left.
    - Background `{tokens.color.canvas}`, text `{tokens.color.ink-subtle}`, type `{tokens.typography.caption}`, padding 64px 32px.
  responsive: |
    ### Breakpoints
    
    | Name | Width | Key Changes |
    |---|---|---|
    | Desktop-XL | 1440px | Default desktop layout |
    | Desktop | 1280px | Card grid 3-up maintained |
    | Tablet | 1024px | Card grid 3-up → 2-up |
    | Mobile-Lg | 768px | Pricing comparison becomes accordion; nav hamburger |
    | Mobile | 480px | Single-column; display-xl scales 80px → ~36px |
    
    ### Touch Targets
    
    - CTAs hold ≥40px tap height across viewports.
    - Pricing tab pills hold ≥36px tap height; touch viewports grow to ≥44px.
    - Form inputs hold ≥44px tap target on touch.
    
    ### Collapsing Strategy
    
    - **Top nav**: links collapse to hamburger below 768px.
    - **Card grids**: 3-up → 2-up at 1024px → 1-up below 768px.
    - **Pricing comparison**: per-tier accordion below 768px.
    - **Display type**: `{tokens.typography.display-xl}` 80px scales toward `{tokens.typography.display-md}` 40px on mobile.
    
    ### Image Behavior
    
    - Product UI screenshots maintain aspect ratio and never crop.
    - Customer logos in the marquee may collapse from 6-up to 3-up below 768px.
  iteration: |
    1. Focus on ONE component at a time and reference it by its `components:` token name.
    2. When introducing a section, decide first which surface lift it lives on.
    3. Default body to `{tokens.typography.body}` at weight 400.
    4. Run `npx @google/design.md lint DESIGN.md` after edits.
    5. Add new variants as separate component entries.
    6. Treat lavender as scarce: brand mark, primary CTA, focus, link emphasis.
    7. Lead every section with a product UI screenshot.
  known_gaps: |
    - The four-step surface ladder values are extracted directly from Linear's `--color-bg-level-3`, `--color-line-tint`, etc. CSS variables; they are Linear's canonical surface spec.
    - Form-field error and validation styling is not visible on the inspected pages.
    - Light mode is not documented because the marketing site does not ship a light theme.
    - Linear's actual product UI uses a richer color-tag palette (red, orange, yellow, green, blue, purple) for issue priorities and project labels — those colors live in the in-product surfaces shown in mockups.
    - The custom display, text, and mono families are proprietary; an open-source substitute is acceptable.
  dos:
    - "Reserve `{tokens.color.canvas}` (#010102) as the system's anchor surface — the faint blue tint is intentional."
    - "Use `{tokens.color.primary}` lavender ONLY for: brand mark, primary CTA, focus ring, link emphasis."
    - Use the four-step surface ladder for hierarchy. Avoid skipping levels.
    - Pair display weight 600 with body weight 400 — Linear resists 700+ display weights.
    - Apply negative letter-spacing aggressively on display.
    - Use product UI screenshots as the protagonist of every section.
    - "Compose CTAs as `{tokens.radius.md}` 8px corners."
  donts:
    - ship a light-mode marketing page.
    - use lavender as a section background or card fill.
    - "introduce a second chromatic accent (orange, pink, green for marketing)."
    - add atmospheric gradients or spotlight cards.
    - pill-round CTAs.
    - "use `#000000` true black as the canvas."
    - combine multiple bright accents in product screenshot mockups.

omitted:
  - section: tokens.elevation
    reason: "Depth model lives in rationale.elevation; add shadow tokens as they stabilize"

integrations:
  shadcn:
    enabled: true
    style: new-york            # legacy style; current styles: see SPEC §7.1
    icon_library: lucide
    css_variables: true
    base_color: neutral
    css: app/globals.css
    components_json: ./components.json
    aliases:
      components: "@/components"
      utils: "@/lib/utils"
      ui: "@/components/ui"
      lib: "@/lib"
      hooks: "@/hooks"
    radius: "8px"
    css_vars:
      theme:
        radius: "8px"
      dark:
        background: "#010102"
        foreground: "#f7f8f8"
        card: "#0f1011"
        card-foreground: "#f7f8f8"
        popover: "#0f1011"
        popover-foreground: "#f7f8f8"
        primary: "#5e6ad2"
        primary-foreground: "#ffffff"
        secondary: "#141516"
        secondary-foreground: "#f7f8f8"
        muted: "#141516"
        muted-foreground: "#d0d6e0"
        accent: "#141516"
        accent-foreground: "#f7f8f8"
        destructive: "#dc2626"
        border: "#23252a"
        input: "#23252a"
        ring: "#5e69d1"
        sidebar: "#010102"
        sidebar-foreground: "#f7f8f8"
        sidebar-primary: "#5e6ad2"
        sidebar-primary-foreground: "#ffffff"
        sidebar-accent: "#141516"
        sidebar-accent-foreground: "#f7f8f8"
        sidebar-border: "#23252a"
        sidebar-ring: "#5e69d1"
    map_from_tokens:
      background: tokens.color.canvas|tokens.color.surface|tokens.color.background
      foreground: tokens.color.ink|tokens.color.text|tokens.color.foreground
      primary: tokens.color.primary
      primary-foreground: tokens.color.on-primary
      border: tokens.color.hairline|tokens.color.border
      ring: tokens.color.primary-focus|tokens.color.primary

policy:
  if_missing: ask
  hierarchy: [typography, spacing, contrast, color]
  reuse:
    prefer_existing_components: true
  accessibility:
    contrast: AA
    focus_visible: required

constraints:
  always:
    - use semantic tokens from this file; never invent off-brand hex
    - bind every listed component property (backgroundColor, textColor, typography, rounded, padding, size, height, width)
    - read rationale.layout / rationale.elevation / rationale.shapes before inventing chrome
    - when shadcn is enabled, apply integrations.shadcn.css_vars to globals.css
    - prefer catalog components over one-off controls
    - "Reserve `{tokens.color.canvas}` (#010102) as the system's anchor surface — the faint blue tint is intentional."
    - "Use `{tokens.color.primary}` lavender ONLY for: brand mark, primary CTA, focus ring, link emphasis."
    - Use the four-step surface ladder for hierarchy. Avoid skipping levels.
    - Pair display weight 600 with body weight 400 — Linear resists 700+ display weights.
    - Apply negative letter-spacing aggressively on display.
    - Use product UI screenshots as the protagonist of every section.
    - "Compose CTAs as `{tokens.radius.md}` 8px corners."
  never:
    - generic purple-on-white AI chrome unrelated to this brand
    - dropping DESIGN.md component variants or rationale during restyle
    - ship a light-mode marketing page.
    - use lavender as a section background or card fill.
    - "introduce a second chromatic accent (orange, pink, green for marketing)."
    - add atmospheric gradients or spotlight cards.
    - pill-round CTAs.
    - "use `#000000` true black as the canvas."
    - combine multiple bright accents in product screenshot mockups.

examples:
  good:
    - ref: https://linear.app
      note: Live brand marketing / product surfaces
    - ref: https://getdesign.md/linear.app/design-md
      note: Source DESIGN.md analysis

provenance:
  owner: AgentsORG
  last_reviewed: "2026-08-06"
  source_material:
    - https://getdesign.md/linear.app/design-md
    - https://github.com/voltagent/awesome-design-md/blob/main/design-md/linear.app/DESIGN.md
    - https://linear.app
    - https://github.com/google-labs-code/design.md/blob/main/docs/spec.md
